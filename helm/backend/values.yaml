replicaCount: 2

image:
  repository: 028185488284.dkr.ecr.eu-central-1.amazonaws.com/platform-dev
  tag: backend-v1.0.0
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080

env:
  CLUSTER_NAME: platform-dev
  AWS_REGION: eu-central-1
  ENVIRONMENT: dev

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi
